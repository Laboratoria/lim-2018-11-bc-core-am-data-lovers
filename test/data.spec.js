require('../src/data.js');


describe('arrayDatos', () => {
  it('es un array de objetos', () => {
    expect(typeof arrayDatos).toBe('array');
  });
});
  /*it('returns `example`', () => {
    expect(example()).toBe('example');
  });
});*/
