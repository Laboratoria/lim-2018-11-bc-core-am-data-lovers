require('../src/data.js');


describe('arrayDatos', () => {
  it('Es un array', () => {
    expect(typeof arrayDatos).toBe('array');
  });
});

/*  it('returns `example`', () => {
    expect(example()).toBe('example');
  });
});
*/
